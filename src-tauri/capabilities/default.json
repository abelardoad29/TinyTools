{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the TinyTools desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "store:default"]
}
